diff --git a/kernel/Kbuild b/kernel/Kbuild
index 5afb4d21..897f27ac 100644
--- a/kernel/Kbuild
+++ b/kernel/Kbuild
@@ -52,7 +52,11 @@ kernelsu-objs += supercall/supercall.o
 
 kernelsu-objs += feature/adb_root.o
 
+ifeq ($(VERSION),4)
+ccflags-y += -DKSU_LEGACY_SELINUX=1
+else
 kernelsu-objs += feature/selinux_hide.o
+endif
 
 ifdef KBUILD_EXTMOD
 ifeq ($(CONFIG_KSU_DISABLE_MANAGER),y)
@@ -89,6 +93,7 @@ KSU_KERNEL_DIR := $(src)
 endif
 
 ccflags-y += -I$(KSU_KERNEL_DIR) -I$(KSU_KERNEL_DIR)/include -I$(KSU_KERNEL_DIR)/..
+ccflags-y += -include $(KSU_KERNEL_DIR)/include/ksu_compat.h
 
 obj-$(CONFIG_KSU) += kernelsu.o
 
diff --git a/kernel/compat/samsung_defex.c b/kernel/compat/samsung_defex.c
index bd384dc8..f011c43a 100644
--- a/kernel/compat/samsung_defex.c
+++ b/kernel/compat/samsung_defex.c
@@ -16,6 +16,7 @@ typedef int (*defex_set_task_creds_t)(struct task_struct *task, unsigned int uid
 
 static defex_get_task_creds_t defex_get_task_creds;
 static defex_set_task_creds_t defex_set_task_creds;
+#ifdef CONFIG_KPROBES
 static bool defex_enforce_hooked;
 
 static int ksu_samsung_defex_pre_handler(struct kprobe *probe, struct pt_regs *regs)
@@ -34,11 +35,14 @@ static struct kprobe defex_enforce_kprobe = {
     .pre_handler = ksu_samsung_defex_pre_handler,
 };
 #endif
+#endif
 
 int ksu_samsung_defex_init(void)
 {
 #ifdef CONFIG_KSU_SAMSUNG_DEFEX
+#ifdef CONFIG_KPROBES
     int ret;
+#endif
 
     defex_get_task_creds = (defex_get_task_creds_t)ksu_resolve_symbol_for_functable_hook("get_task_creds");
     defex_set_task_creds = (defex_set_task_creds_t)ksu_resolve_symbol_for_functable_hook("set_task_creds");
@@ -47,14 +51,17 @@ int ksu_samsung_defex_init(void)
         return -ENOENT;
     }
 
+#ifdef CONFIG_KPROBES
     ret = register_kprobe(&defex_enforce_kprobe);
     if (ret) {
         pr_err("Samsung DEFEX enforce hook unavailable: %d\n", ret);
         return ret;
     }
     defex_enforce_hooked = true;
-
     pr_info("Samsung DEFEX credential synchronization and KSU-task bypass enabled\n");
+#else
+    pr_info("Samsung DEFEX credential synchronization enabled without kprobe bypass\n");
+#endif
 #endif
     return 0;
 }
@@ -62,11 +69,13 @@ int ksu_samsung_defex_init(void)
 void ksu_samsung_defex_exit(void)
 {
 #ifdef CONFIG_KSU_SAMSUNG_DEFEX
+#ifdef CONFIG_KPROBES
     if (defex_enforce_hooked) {
         unregister_kprobe(&defex_enforce_kprobe);
         defex_enforce_hooked = false;
     }
 #endif
+#endif
 }
 
 void ksu_samsung_defex_sync_current(void)
diff --git a/kernel/core/init.c b/kernel/core/init.c
index f16235e2..15ae98b2 100644
--- a/kernel/core/init.c
+++ b/kernel/core/init.c
@@ -247,8 +247,10 @@ module_exit(kernelsu_exit);
 MODULE_LICENSE("GPL");
 MODULE_AUTHOR("weishu");
 MODULE_DESCRIPTION("Android KernelSU");
+#ifdef MODULE_IMPORT_NS
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 13, 0)
 MODULE_IMPORT_NS("VFS_internal_I_am_really_a_filesystem_and_am_NOT_a_driver");
 #else
 MODULE_IMPORT_NS(VFS_internal_I_am_really_a_filesystem_and_am_NOT_a_driver);
 #endif
+#endif
diff --git a/kernel/feature/kernel_umount.c b/kernel/feature/kernel_umount.c
index f914ff82..20b5315b 100644
--- a/kernel/feature/kernel_umount.c
+++ b/kernel/feature/kernel_umount.c
@@ -1,5 +1,6 @@
 #include <linux/sched.h>
 #include <linux/slab.h>
+#include <linux/syscalls.h>
 #include <linux/task_work.h>
 #include <linux/cred.h>
 #include <linux/fs.h>
@@ -41,11 +42,22 @@ static const struct ksu_feature_handler kernel_umount_handler = {
     .set_handler = kernel_umount_feature_set,
 };
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
 extern int path_umount(struct path *path, int flags);
+#endif
 
 static void ksu_umount_mnt(const char *mnt, struct path *path, int flags)
 {
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    mm_segment_t old_fs = get_fs();
+    int err;
+
+    set_fs(KERNEL_DS);
+    err = ksys_umount((char __user *)mnt, flags);
+    set_fs(old_fs);
+#else
     int err = path_umount(path, flags);
+#endif
     if (err) {
         pr_info("umount %s failed: %d\n", mnt, err);
     }
diff --git a/kernel/feature/selinux_hide.h b/kernel/feature/selinux_hide.h
index 5a043710..7433072a 100644
--- a/kernel/feature/selinux_hide.h
+++ b/kernel/feature/selinux_hide.h
@@ -1,10 +1,18 @@
 #ifndef __KSU_H_SELINUX_HIDE
 #define __KSU_H_SELINUX_HIDE
 
+#ifdef KSU_LEGACY_SELINUX
+static inline void ksu_selinux_hide_init(void) {}
+static inline void ksu_selinux_hide_exit(void) {}
+static inline void ksu_selinux_hide_drop_backup_if_unused(void) {}
+static inline void ksu_selinux_hide_handle_second_stage(void) {}
+static inline void ksu_selinux_hide_handle_post_fs_data(void) {}
+#else
 void ksu_selinux_hide_init();
 void ksu_selinux_hide_exit();
 void ksu_selinux_hide_drop_backup_if_unused();
 void ksu_selinux_hide_handle_second_stage();
 void ksu_selinux_hide_handle_post_fs_data();
+#endif
 
 #endif
diff --git a/kernel/feature/sucompat.c b/kernel/feature/sucompat.c
index 239fcf2a..3508019d 100644
--- a/kernel/feature/sucompat.c
+++ b/kernel/feature/sucompat.c
@@ -4,7 +4,6 @@
 #include <linux/preempt.h>
 #include <linux/printk.h>
 #include <linux/mm.h>
-#include <linux/pgtable.h>
 #include <linux/uaccess.h>
 #include <asm/current.h>
 #include <linux/cred.h>
diff --git a/kernel/include/ksu_compat.h b/kernel/include/ksu_compat.h
new file mode 100644
index 00000000..fae642e7
--- /dev/null
+++ b/kernel/include/ksu_compat.h
@@ -0,0 +1,71 @@
+#ifndef __KSU_COMPAT_H
+#define __KSU_COMPAT_H
+
+#include <linux/uaccess.h>
+#include <linux/version.h>
+#include <linux/string.h>
+
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+#include <asm/unistd.h>
+#ifndef SECCOMP_ARCH_NATIVE_NR
+#define SECCOMP_ARCH_NATIVE_NR __NR_syscalls
+#endif
+#ifndef TWA_RESUME
+#define TWA_RESUME true
+#endif
+#endif
+
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+static inline ssize_t ksu_strscpy_pad(char *dst, const char *src, size_t count)
+{
+    size_t src_len;
+
+    if (!count)
+        return -E2BIG;
+
+    src_len = strlcpy(dst, src, count);
+    if (src_len >= count)
+        return -E2BIG;
+
+    memset(dst + src_len, 0, count - src_len);
+    return src_len;
+}
+
+#define strscpy_pad(dst, src, count) \
+    ksu_strscpy_pad((dst), (src), (count))
+#endif
+
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 8, 0)
+static inline long ksu_copy_from_user_nofault(void *dst, const void __user *src,
+                                              size_t size)
+{
+    unsigned long not_copied;
+
+    pagefault_disable();
+    not_copied = __copy_from_user_inatomic(dst, src, size);
+    pagefault_enable();
+    return not_copied ? -EFAULT : 0;
+}
+
+static inline long ksu_copy_to_user_nofault(void __user *dst, const void *src,
+                                            size_t size)
+{
+    unsigned long not_copied;
+
+    pagefault_disable();
+    not_copied = __copy_to_user_inatomic(dst, src, size);
+    pagefault_enable();
+    return not_copied ? -EFAULT : 0;
+}
+
+#define copy_from_user_nofault(dst, src, size) \
+    ksu_copy_from_user_nofault((dst), (src), (size))
+#define copy_to_user_nofault(dst, src, size) \
+    ksu_copy_to_user_nofault((dst), (src), (size))
+#define copy_to_kernel_nofault(dst, src, size) \
+    probe_kernel_write((dst), (src), (size))
+#define strncpy_from_user_nofault(dst, src, count) \
+    strncpy_from_unsafe((dst), (const void *)(src), (count))
+#endif
+
+#endif
diff --git a/kernel/infra/file_wrapper.c b/kernel/infra/file_wrapper.c
index 66ea2958..35aa0c4f 100644
--- a/kernel/infra/file_wrapper.c
+++ b/kernel/infra/file_wrapper.c
@@ -84,6 +84,7 @@ static ssize_t ksu_wrapper_write_iter(struct kiocb *iocb, struct iov_iter *iovi)
     return orig->f_op->write_iter(iocb, iovi);
 }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0)
 static int ksu_wrapper_iopoll(struct kiocb *kiocb, struct io_comp_batch *icb, unsigned int v)
 {
@@ -101,6 +102,7 @@ static int ksu_wrapper_iopoll(struct kiocb *kiocb, bool spin)
     return orig->f_op->iopoll(kiocb, spin);
 }
 #endif
+#endif
 
 #if LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 0)
 static int ksu_wrapper_iterate(struct file *fp, struct dir_context *dc)
@@ -302,6 +304,7 @@ static ssize_t ksu_wrapper_copy_file_range(struct file *file_in, loff_t pos_in,
     return orig->f_op->copy_file_range(file_in, pos_in, orig, pos_out, len, flags);
 }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
 // no REMAP_FILE_DEDUP: use file_in
 // https://cs.android.com/android/kernel/superproject/+/common-android-mainline:common/fs/read_write.c;l=1598-1599;drc=398da7defe218d3e51b0f3bdff75147e28125b60
 // https://cs.android.com/android/kernel/superproject/+/common-android-mainline:common/fs/remap_range.c;l=403-404;drc=398da7defe218d3e51b0f3bdff75147e28125b60
@@ -320,6 +323,23 @@ static loff_t ksu_wrapper_remap_file_range(struct file *file_in, loff_t pos_in,
         return orig->f_op->remap_file_range(orig, pos_in, file_out, pos_out, len, remap_flags);
     }
 }
+#else
+static int ksu_wrapper_clone_file_range(struct file *file_in, loff_t pos_in,
+                                        struct file *file_out, loff_t pos_out, u64 len)
+{
+    struct ksu_file_wrapper *data = file_in->private_data;
+    struct file *orig = data->orig;
+    return orig->f_op->clone_file_range(orig, pos_in, file_out, pos_out, len);
+}
+
+static int ksu_wrapper_dedupe_file_range(struct file *file_in, loff_t pos_in,
+                                         struct file *file_out, loff_t pos_out, u64 len)
+{
+    struct ksu_file_wrapper *data = file_out->private_data;
+    struct file *orig = data->orig;
+    return orig->f_op->dedupe_file_range(file_in, pos_in, orig, pos_out, len);
+}
+#endif
 
 static int ksu_wrapper_fadvise(struct file *fp, loff_t off1, loff_t off2, int flags)
 {
@@ -360,7 +380,9 @@ static struct ksu_file_wrapper *ksu_create_file_wrapper(struct file *fp)
     p->ops.write = fp->f_op->write ? ksu_wrapper_write : NULL;
     p->ops.read_iter = fp->f_op->read_iter ? ksu_wrapper_read_iter : NULL;
     p->ops.write_iter = fp->f_op->write_iter ? ksu_wrapper_write_iter : NULL;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
     p->ops.iopoll = fp->f_op->iopoll ? ksu_wrapper_iopoll : NULL;
+#endif
 #if LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 0)
     p->ops.iterate = fp->f_op->iterate ? ksu_wrapper_iterate : NULL;
 #endif
@@ -391,7 +413,12 @@ static struct ksu_file_wrapper *ksu_create_file_wrapper(struct file *fp)
     p->ops.fallocate = fp->f_op->fallocate ? ksu_wrapper_fallocate : NULL;
     p->ops.show_fdinfo = fp->f_op->show_fdinfo ? ksu_wrapper_show_fdinfo : NULL;
     p->ops.copy_file_range = fp->f_op->copy_file_range ? ksu_wrapper_copy_file_range : NULL;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
     p->ops.remap_file_range = fp->f_op->remap_file_range ? ksu_wrapper_remap_file_range : NULL;
+#else
+    p->ops.clone_file_range = fp->f_op->clone_file_range ? ksu_wrapper_clone_file_range : NULL;
+    p->ops.dedupe_file_range = fp->f_op->dedupe_file_range ? ksu_wrapper_dedupe_file_range : NULL;
+#endif
     p->ops.fadvise = fp->f_op->fadvise ? ksu_wrapper_fadvise : NULL;
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 6, 0)
@@ -437,7 +464,9 @@ static struct vfsmount *anon_inode_mnt __read_mostly;
 static struct inode *ksu_anon_inode_make_secure_inode(const char *name, const struct inode *context_inode)
 {
     struct inode *inode;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     const struct qstr qname = QSTR_INIT(name, strlen(name));
+#endif
     int error;
 
     if (unlikely(!anon_inode_mnt)) {
@@ -448,7 +477,21 @@ static struct inode *ksu_anon_inode_make_secure_inode(const char *name, const st
     if (IS_ERR(inode))
         return inode;
     inode->i_flags &= ~S_PRIVATE;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     error = security_inode_init_security_anon(inode, &qname, context_inode);
+#else
+    if (context_inode) {
+        error = -EOPNOTSUPP;
+    } else if (inode->i_security) {
+        struct inode_security_struct *isec = inode->i_security;
+        isec->sclass = SECCLASS_FILE;
+        isec->sid = ksu_file_sid;
+        isec->initialized = LABEL_INITIALIZED;
+        error = 0;
+    } else {
+        error = -EACCES;
+    }
+#endif
     if (error) {
         iput(inode);
         return ERR_PTR(error);
@@ -523,7 +566,11 @@ int ksu_install_file_wrapper(int fd)
     struct inode *wrapper_inode = file_inode(wrapper_file);
     // libc's stdio relies on the fstat() result of the fd to determine its buffer type.
     wrapper_inode->i_mode = file_inode(orig_file)->i_mode;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     struct inode_security_struct *wrapper_sec = selinux_inode(wrapper_inode);
+#else
+    struct inode_security_struct *wrapper_sec = wrapper_inode->i_security;
+#endif
     // Use ksu_file_sid to bypass SELinux check.
     // When we call `su` from terminal app, this is useful.
     if (wrapper_sec) {
diff --git a/kernel/infra/su_mount_ns.c b/kernel/infra/su_mount_ns.c
index 0d459215..a2c5a017 100644
--- a/kernel/infra/su_mount_ns.c
+++ b/kernel/infra/su_mount_ns.c
@@ -13,7 +13,7 @@
 #include <linux/syscalls.h>
 #include <linux/task_work.h>
 #include <linux/version.h>
-#include <uapi/linux/mount.h>
+#include <linux/mount.h>
 
 #include "arch.h"
 #include "klog.h" // IWYU pragma: keep
@@ -21,8 +21,10 @@
 #include "infra/su_mount_ns.h"
 #include "util.h"
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
 extern int path_mount(const char *dev_name, struct path *path, const char *type_page, unsigned long flags,
                       void *data_page);
+#endif
 
 #if defined(__aarch64__)
 extern long __arm64_sys_setns(const struct pt_regs *regs);
@@ -146,10 +148,19 @@ static void ksu_mnt_ns_individual(void)
     }
 
     // make root mount private
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    mm_segment_t old_fs = get_fs();
+    int pm_ret;
+
+    set_fs(KERNEL_DS);
+    pm_ret = ksys_mount(NULL, (char __user *)"/", NULL, MS_PRIVATE | MS_REC, NULL);
+    set_fs(old_fs);
+#else
     struct path root_path;
     get_fs_root(current->fs, &root_path);
     int pm_ret = path_mount(NULL, &root_path, NULL, MS_PRIVATE | MS_REC, NULL);
     path_put(&root_path);
+#endif
 
     if (pm_ret < 0) {
         pr_err("failed to make root private, err: %d\n", pm_ret);
diff --git a/kernel/manager/pkg_observer.c b/kernel/manager/pkg_observer.c
index 512b9929..f3b071bd 100644
--- a/kernel/manager/pkg_observer.c
+++ b/kernel/manager/pkg_observer.c
@@ -21,23 +21,41 @@ struct watch_dir {
 
 static struct fsnotify_group *g;
 
-static int ksu_handle_inode_event(struct fsnotify_mark *mark, u32 mask, struct inode *inode, struct inode *dir,
-                                  const struct qstr *file_name, u32 cookie)
+static int ksu_handle_package_event(u32 mask, const unsigned char *file_name, size_t file_name_len)
 {
     if (!file_name)
         return 0;
     if (mask & FS_ISDIR)
         return 0;
-    if (file_name->len == 13 && !memcmp(file_name->name, "packages.list", 13)) {
+    if (file_name_len == 13 && !memcmp(file_name, "packages.list", 13)) {
         pr_info("packages.list detected: %d\n", mask);
         track_throne(false);
     }
     return 0;
 }
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 10, 0)
+static int ksu_handle_event(struct fsnotify_group *group, struct inode *inode, u32 mask, const void *data,
+                            int data_type, const unsigned char *file_name, u32 cookie,
+                            struct fsnotify_iter_info *iter_info)
+{
+    return ksu_handle_package_event(mask, file_name, file_name ? strlen(file_name) : 0);
+}
+
+static const struct fsnotify_ops ksu_ops = {
+    .handle_event = ksu_handle_event,
+};
+#else
+static int ksu_handle_inode_event(struct fsnotify_mark *mark, u32 mask, struct inode *inode, struct inode *dir,
+                                  const struct qstr *file_name, u32 cookie)
+{
+    return ksu_handle_package_event(mask, file_name ? file_name->name : NULL, file_name ? file_name->len : 0);
+}
+
 static const struct fsnotify_ops ksu_ops = {
     .handle_inode_event = ksu_handle_inode_event,
 };
+#endif
 
 static int add_mark_on_inode(struct inode *inode, u32 mask, struct fsnotify_mark **out)
 {
@@ -121,4 +139,4 @@ void __exit ksu_observer_exit(void)
     unwatch_one_dir(&g_watch);
     fsnotify_put_group(g);
     pr_info("observer exit done\n");
-}
\ No newline at end of file
+}
diff --git a/kernel/policy/allowlist.c b/kernel/policy/allowlist.c
index 89e8d089..cfd00523 100644
--- a/kernel/policy/allowlist.c
+++ b/kernel/policy/allowlist.c
@@ -11,6 +11,7 @@
 #include <linux/list.h>
 #include <linux/printk.h>
 #include <linux/slab.h>
+#include <linux/sched/task.h>
 #include <linux/types.h>
 #include <linux/version.h>
 #include <linux/compiler_types.h>
@@ -473,7 +474,11 @@ void ksu_persistent_allow_list()
         goto put_task;
     }
     cb->func = do_persistent_allow_list;
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    if (task_work_add(tsk, cb, true)) {
+#else
     if (task_work_add(tsk, cb, TWA_RESUME)) {
+#endif
         kfree(cb);
         pr_warn("save_allow_list add task_work failed\n");
     }
@@ -496,7 +501,7 @@ static void migrate_profile(u32 version, struct app_profile *profile)
                 pr_info("migrated domain of profile: %s\n", profile->key);
             }
         }
-        fallthrough;
+        /* fall through */
     case 3:
         if (profile->allow_su) {
             profile->rp_config.profile.flags = FLAG_KSU_NO_NEW_PRIVS;
diff --git a/kernel/policy/app_profile.c b/kernel/policy/app_profile.c
index 256d6442..0e408d82 100644
--- a/kernel/policy/app_profile.c
+++ b/kernel/policy/app_profile.c
@@ -63,7 +63,11 @@ void setup_groups(struct root_profile *profile, struct cred *cred)
     put_group_info(group_info);
 }
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+void put_seccomp_filter(struct task_struct *tsk);
+#else
 void seccomp_filter_release(struct task_struct *tsk);
+#endif
 
 static void disable_seccomp(void)
 {
@@ -89,7 +93,9 @@ static void disable_seccomp(void)
 
     current->seccomp.mode = 0;
     current->seccomp.filter = NULL;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 0, 0)
     atomic_set(&current->seccomp.filter_count, 0);
+#endif
     spin_unlock_irq(&current->sighand->siglock);
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 11, 0)
@@ -100,7 +106,11 @@ static void disable_seccomp(void)
     fake->sighand = NULL;
 #endif
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    put_seccomp_filter(fake);
+#else
     seccomp_filter_release(fake);
+#endif
     kfree(fake);
 }
 
diff --git a/kernel/selinux/rules.c b/kernel/selinux/rules.c
index 762a7c55..34ce9d49 100644
--- a/kernel/selinux/rules.c
+++ b/kernel/selinux/rules.c
@@ -44,13 +44,16 @@ static void reset_avc_cache()
 
 void apply_kernelsu_rules()
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     struct selinux_policy *pol, *old_pol = selinux_state.policy;
+#endif
     struct policydb *db;
 
     if (!getenforce()) {
         pr_info("SELinux permissive or disabled, apply rules!\n");
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     mutex_lock(&selinux_state.policy_mutex);
     backup_sepolicy =
         ksu_dup_sepolicy(rcu_dereference_protected(old_pol, lockdep_is_held(&selinux_state.policy_mutex)));
@@ -82,6 +85,10 @@ void apply_kernelsu_rules()
     }
 
     db = &pol->policydb;
+#else
+    rcu_read_lock();
+    db = &rcu_dereference(selinux_state.ss)->policydb;
+#endif
 
     ksu_type(db, KERNEL_SU_DOMAIN, "domain");
     ksu_permissive(db, KERNEL_SU_DOMAIN);
@@ -155,13 +162,18 @@ void apply_kernelsu_rules()
     ksu_allow(db, "system_server", KERNEL_SU_DOMAIN, "process", "getpgid");
     ksu_allow(db, "system_server", KERNEL_SU_DOMAIN, "process", "sigkill");
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     rcu_assign_pointer(selinux_state.policy, pol);
     synchronize_rcu();
     ksu_destroy_sepolicy(old_pol);
-
+#endif
     reset_avc_cache();
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
 out_unlock:
     mutex_unlock(&selinux_state.policy_mutex);
+#else
+    rcu_read_unlock();
+#endif
 }
 
 #define KSU_SEPOLICY_MAX_BATCH_SIZE (8U * 1024U * 1024U)
@@ -431,7 +443,9 @@ static int apply_one_sepolicy_cmd(struct policydb *db, const struct sepol_data *
 
 int handle_sepolicy(void __user *user_data, u64 data_len)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     struct selinux_policy *pol, *old_pol;
+#endif
     struct policydb *db;
     struct sepol_batch_cursor cursor;
     u8 *payload;
@@ -461,8 +475,8 @@ int handle_sepolicy(void __user *user_data, u64 data_len)
         pr_info("SELinux permissive or disabled when handle policy!\n");
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     mutex_lock(&selinux_state.policy_mutex);
-
     old_pol = selinux_state.policy;
     pol = ksu_dup_sepolicy(rcu_dereference_protected(old_pol, lockdep_is_held(&selinux_state.policy_mutex)));
     if (IS_ERR(pol)) {
@@ -471,6 +485,10 @@ int handle_sepolicy(void __user *user_data, u64 data_len)
         goto out_unlock;
     }
     db = &pol->policydb;
+#else
+    rcu_read_lock();
+    db = &rcu_dereference(selinux_state.ss)->policydb;
+#endif
 
     cursor.cur = payload;
     cursor.end = payload + (size_t)data_len;
@@ -514,18 +532,25 @@ int handle_sepolicy(void __user *user_data, u64 data_len)
         cmd_index++;
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     rcu_assign_pointer(selinux_state.policy, pol);
     synchronize_rcu();
     ksu_destroy_sepolicy(old_pol);
-
+#endif
     reset_avc_cache();
     ret = success_cmd_count;
     goto out_unlock;
 
 out_drop_new_policy:
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     ksu_destroy_sepolicy(pol);
+#endif
 out_unlock:
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     mutex_unlock(&selinux_state.policy_mutex);
+#else
+    rcu_read_unlock();
+#endif
 out_free:
     kvfree(payload);
 
diff --git a/kernel/selinux/selinux.c b/kernel/selinux/selinux.c
index 7bc6d6d0..0b28c420 100644
--- a/kernel/selinux/selinux.c
+++ b/kernel/selinux/selinux.c
@@ -32,7 +32,11 @@ static int transive_to_domain(const char *domain, struct cred *cred, bool clear_
 #else
     struct cred_security_struct *tsec;
 #endif
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    tsec = cred->security;
+#else
     tsec = selinux_cred(cred);
+#endif
     if (!tsec) {
         pr_err("tsec == NULL!\n");
         return -1;
@@ -161,7 +165,11 @@ static bool is_sid_match(const struct cred *cred, u32 cached_sid, const char *fa
         return false;
     }
 #if LINUX_VERSION_CODE < KERNEL_VERSION(6, 18, 0)
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 0, 0)
+    const struct task_security_struct *tsec = cred->security;
+#else
     const struct task_security_struct *tsec = selinux_cred(cred);
+#endif
 #else
     const struct cred_security_struct *tsec = selinux_cred(cred);
 #endif
diff --git a/kernel/selinux/sepolicy.c b/kernel/selinux/sepolicy.c
index 6bd3df18..07e89080 100644
--- a/kernel/selinux/sepolicy.c
+++ b/kernel/selinux/sepolicy.c
@@ -161,14 +161,23 @@ static bool remove_avtab_node(struct policydb *db, struct avtab_node *node)
 
     for (i = 0; i < db->te_avtab.nslot; i++) {
         prev = NULL;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0)
         for (n = db->te_avtab.htable[i]; n; prev = n, n = n->next) {
+#else
+        for (n = flex_array_get_ptr(db->te_avtab.htable, i); n; prev = n, n = n->next) {
+#endif
             if (n != node)
                 continue;
 
             if (prev)
                 prev->next = n->next;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0)
             else
                 db->te_avtab.htable[i] = n->next;
+#else
+            else if (flex_array_put_ptr(db->te_avtab.htable, i, n->next, GFP_ATOMIC | __GFP_ZERO))
+                goto out_destroy_removed;
+#endif
 
             if (db->te_avtab.nel > 0)
                 db->te_avtab.nel--;
@@ -177,7 +186,12 @@ static bool remove_avtab_node(struct policydb *db, struct avtab_node *node)
                 shrink_size += sizeof(u8) + sizeof(u8) + sizeof(u32) * ARRAY_SIZE(n->datum.u.xperms->perms.p);
             }
             n->next = NULL;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 8, 0)
             removed.htable[0] = n;
+#else
+            if (flex_array_put_ptr(removed.htable, 0, n, GFP_ATOMIC | __GFP_ZERO))
+                goto out_destroy_removed;
+#endif
             removed.nel = 1;
             avtab_destroy(&removed);
             if (db->len >= shrink_size)
@@ -186,6 +200,7 @@ static bool remove_avtab_node(struct policydb *db, struct avtab_node *node)
         }
     }
 
+out_destroy_removed:
     avtab_destroy(&removed);
     return false;
 }
@@ -564,6 +579,7 @@ static bool add_filename_trans(struct policydb *db, const char *s, const char *t
         return false;
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 7, 0)
     struct filename_trans_key key;
     key.ttype = tgt->value;
     key.tclass = cls->value;
@@ -596,6 +612,49 @@ static bool add_filename_trans(struct policydb *db, const char *s, const char *t
 
     db->compat_filename_trans_count++;
     return ebitmap_set_bit(&trans->stypes, src->value - 1, 1) == 0;
+#else
+    struct filename_trans key;
+    struct filename_trans_datum *trans;
+
+    key.ttype = tgt->value;
+    key.tclass = cls->value;
+    key.name = (char *)o;
+
+    trans = hashtab_search(db->filename_trans, &key);
+    if (!trans) {
+        struct filename_trans *new_key;
+
+        trans = kzalloc(sizeof(*trans), GFP_ATOMIC);
+        if (!trans)
+            return false;
+
+        new_key = kmalloc(sizeof(*new_key), GFP_ATOMIC);
+        if (!new_key) {
+            kfree(trans);
+            return false;
+        }
+
+        *new_key = key;
+        new_key->name = kstrdup(key.name, GFP_ATOMIC);
+        if (!new_key->name) {
+            kfree(new_key);
+            kfree(trans);
+            return false;
+        }
+
+        trans->otype = def->value;
+        if (hashtab_insert(db->filename_trans, new_key, trans)) {
+            kfree(new_key->name);
+            kfree(new_key);
+            kfree(trans);
+            return false;
+        }
+    } else {
+        trans->otype = def->value;
+    }
+
+    return ebitmap_set_bit(&db->filename_trans_ttypes, src->value - 1, 1) == 0;
+#endif
 }
 
 static bool add_genfscon(struct policydb *db, const char *fs_name, const char *path, const char *context)
@@ -658,6 +717,7 @@ static bool add_type(struct policydb *db, const char *type_name, bool attr)
         return false;
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 1, 0)
     struct ebitmap *new_type_attr_map_array =
         ksu_kvrealloc(db->type_attr_map_array, value * sizeof(struct ebitmap), (value - 1) * sizeof(struct ebitmap));
 
@@ -697,6 +757,63 @@ static bool add_type(struct policydb *db, const char *type_name, bool attr)
     }
 
     return true;
+#else
+    struct flex_array *new_type_attr_map_array;
+    struct flex_array *new_type_val_to_struct;
+    struct flex_array *new_val_to_name_types;
+    struct flex_array *old_fa;
+    void *old_elem;
+    int i;
+
+    new_type_attr_map_array = flex_array_alloc(sizeof(struct ebitmap), value,
+                                                GFP_ATOMIC | __GFP_ZERO);
+    new_type_val_to_struct = flex_array_alloc(sizeof(struct type_datum *), value,
+                                               GFP_ATOMIC | __GFP_ZERO);
+    new_val_to_name_types = flex_array_alloc(sizeof(char *), value,
+                                              GFP_ATOMIC | __GFP_ZERO);
+    if (!new_type_attr_map_array || !new_type_val_to_struct || !new_val_to_name_types)
+        return false;
+
+    if (flex_array_prealloc(new_type_attr_map_array, 0, value, GFP_ATOMIC | __GFP_ZERO) ||
+        flex_array_prealloc(new_type_val_to_struct, 0, value, GFP_ATOMIC | __GFP_ZERO) ||
+        flex_array_prealloc(new_val_to_name_types, 0, value, GFP_ATOMIC | __GFP_ZERO))
+        return false;
+
+    for (i = 0; i < value - 1; i++) {
+        old_elem = flex_array_get(db->type_attr_map_array, i);
+        if (old_elem)
+            flex_array_put(new_type_attr_map_array, i, old_elem, GFP_ATOMIC | __GFP_ZERO);
+
+        old_elem = flex_array_get_ptr(db->type_val_to_struct_array, i);
+        if (old_elem)
+            flex_array_put_ptr(new_type_val_to_struct, i, old_elem, GFP_ATOMIC | __GFP_ZERO);
+
+        old_elem = flex_array_get_ptr(db->sym_val_to_name[SYM_TYPES], i);
+        if (old_elem)
+            flex_array_put_ptr(new_val_to_name_types, i, old_elem, GFP_ATOMIC | __GFP_ZERO);
+    }
+
+    old_fa = db->type_attr_map_array;
+    db->type_attr_map_array = new_type_attr_map_array;
+    flex_array_free(old_fa);
+    ebitmap_init(flex_array_get(db->type_attr_map_array, value - 1));
+    ebitmap_set_bit(flex_array_get(db->type_attr_map_array, value - 1), value - 1, 1);
+
+    old_fa = db->type_val_to_struct_array;
+    db->type_val_to_struct_array = new_type_val_to_struct;
+    flex_array_free(old_fa);
+    flex_array_put_ptr(db->type_val_to_struct_array, value - 1, type, GFP_ATOMIC | __GFP_ZERO);
+
+    old_fa = db->sym_val_to_name[SYM_TYPES];
+    db->sym_val_to_name[SYM_TYPES] = new_val_to_name_types;
+    flex_array_free(old_fa);
+    flex_array_put_ptr(db->sym_val_to_name[SYM_TYPES], value - 1, key, GFP_ATOMIC | __GFP_ZERO);
+
+    for (i = 0; i < db->p_roles.nprim; i++)
+        ebitmap_set_bit(&db->role_val_to_struct[i]->types, value - 1, 1);
+
+    return true;
+#endif
 }
 
 static bool set_type_state(struct policydb *db, const char *type_name, bool permissive)
@@ -726,7 +843,11 @@ static bool set_type_state(struct policydb *db, const char *type_name, bool perm
 
 static void add_typeattribute_raw(struct policydb *db, struct type_datum *type, struct type_datum *attr)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 1, 0)
     struct ebitmap *sattr = &db->type_attr_map_array[type->value - 1];
+#else
+    struct ebitmap *sattr = flex_array_get(db->type_attr_map_array, type->value - 1);
+#endif
     ebitmap_set_bit(sattr, attr->value - 1, 1);
 
     struct hashtab_node *node;
@@ -867,6 +988,7 @@ bool ksu_genfscon(struct policydb *db, const char *fs_name, const char *path, co
 
 // ======== sepolicy ========
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
 void ksu_destroy_sepolicy(struct selinux_policy *pol)
 {
     policydb_destroy(&pol->policydb);
@@ -946,3 +1068,4 @@ out_free_data:
 
     return ERR_PTR(ret);
 }
+#endif
diff --git a/kernel/sulog/event.c b/kernel/sulog/event.c
index 51d6fa84..dd75195f 100644
--- a/kernel/sulog/event.c
+++ b/kernel/sulog/event.c
@@ -2,7 +2,7 @@
 #include <linux/compat.h>
 #include <linux/cred.h>
 #include <linux/gfp.h>
-#include <linux/minmax.h>
+#include <linux/kernel.h>
 #include <linux/overflow.h>
 #include <linux/sched/signal.h>
 #include <linux/slab.h>
diff --git a/kernel/supercall/dispatch.c b/kernel/supercall/dispatch.c
index 68c333f1..04df9b03 100644
--- a/kernel/supercall/dispatch.c
+++ b/kernel/supercall/dispatch.c
@@ -1,6 +1,8 @@
 #include <linux/capability.h>
 #include <linux/cred.h>
 #include <linux/slab.h>
+#include <linux/sched/signal.h>
+#include <linux/sched/task.h>
 #include <linux/uaccess.h>
 #include <linux/version.h>
 #include <linux/thread_info.h>
diff --git a/kernel/supercall/supercall.c b/kernel/supercall/supercall.c
index 3dfd4abf..b4095052 100644
--- a/kernel/supercall/supercall.c
+++ b/kernel/supercall/supercall.c
@@ -3,6 +3,7 @@
 #include <linux/fdtable.h>
 #include <linux/file.h>
 #include <linux/fs.h>
+#include <linux/kernel.h>
 #include <linux/kprobes.h>
 #include <linux/pid.h>
 #include <linux/slab.h>
@@ -10,6 +11,11 @@
 #include <linux/task_work.h>
 #include <linux/uaccess.h>
 #include <linux/version.h>
+#include <asm/syscall.h>
+
+#if !defined(CONFIG_KPROBES) && defined(CONFIG_HAVE_SYSCALL_TRACEPOINTS)
+#include <trace/events/syscalls.h>
+#endif
 
 #include "uapi/supercall.h"
 #include "supercall/internal.h"
@@ -77,19 +83,23 @@ static void ksu_install_fd_tw_func(struct callback_head *cb)
     kfree(tw);
 }
 
-static int reboot_handler_pre(struct kprobe *p, struct pt_regs *regs)
+static void ksu_queue_install_fd(struct pt_regs *regs)
 {
-    struct pt_regs *real_regs = PT_REAL_REGS(regs);
-    int magic1 = (int)PT_REGS_PARM1(real_regs);
-    int magic2 = (int)PT_REGS_PARM2(real_regs);
+    unsigned long args[4];
+    int magic1;
+    int magic2;
+
+    syscall_get_arguments(current, regs, 0, ARRAY_SIZE(args), args);
+    magic1 = (int)args[0];
+    magic2 = (int)args[1];
 
     if (magic1 == KSU_INSTALL_MAGIC1 && magic2 == KSU_INSTALL_MAGIC2) {
         struct ksu_install_fd_tw *tw;
-        unsigned long arg4 = (unsigned long)PT_REGS_SYSCALL_PARM4(real_regs);
+        unsigned long arg4 = args[3];
 
         tw = kzalloc(sizeof(*tw), GFP_ATOMIC);
         if (!tw)
-            return 0;
+            return;
 
         tw->outp = (int __user *)arg4;
         tw->cb.func = ksu_install_fd_tw_func;
@@ -99,6 +109,15 @@ static int reboot_handler_pre(struct kprobe *p, struct pt_regs *regs)
             pr_warn("install fd add task_work failed\n");
         }
     }
+}
+
+#ifdef CONFIG_KPROBES
+static int reboot_handler_pre(struct kprobe *p, struct pt_regs *regs)
+{
+    struct pt_regs *real_regs = PT_REAL_REGS(regs);
+
+    (void)p;
+    ksu_queue_install_fd(real_regs);
 
     return 0;
 }
@@ -107,23 +126,57 @@ static struct kprobe reboot_kp = {
     .symbol_name = REBOOT_SYMBOL,
     .pre_handler = reboot_handler_pre,
 };
+#elif defined(CONFIG_HAVE_SYSCALL_TRACEPOINTS)
+static bool reboot_trace_registered;
+
+static void ksu_reboot_sys_enter(void *data, struct pt_regs *regs, long id)
+{
+    (void)data;
+    if (id == __NR_reboot)
+        ksu_queue_install_fd(regs);
+}
+#endif
 
 void __init ksu_supercalls_init(void)
 {
+#if defined(CONFIG_KPROBES)
+    int rc;
+#elif defined(CONFIG_HAVE_SYSCALL_TRACEPOINTS)
     int rc;
+#endif
 
     ksu_supercall_dump_commands();
 
+#if defined(CONFIG_KPROBES)
     rc = register_kprobe(&reboot_kp);
     if (rc) {
         pr_err("reboot kprobe failed: %d\n", rc);
     } else {
         pr_info("reboot kprobe registered successfully\n");
     }
+#elif defined(CONFIG_HAVE_SYSCALL_TRACEPOINTS)
+    rc = register_trace_sys_enter(ksu_reboot_sys_enter, NULL);
+    if (rc) {
+        pr_err("reboot syscall tracepoint failed: %d\n", rc);
+    } else {
+        reboot_trace_registered = true;
+        pr_info("reboot syscall tracepoint registered successfully\n");
+    }
+#else
+    pr_err("no kprobe or syscall tracepoint support for supercalls\n");
+#endif
 }
 
 void __exit ksu_supercalls_exit(void)
 {
+#if defined(CONFIG_KPROBES)
     unregister_kprobe(&reboot_kp);
+#elif defined(CONFIG_HAVE_SYSCALL_TRACEPOINTS)
+    if (reboot_trace_registered) {
+        unregister_trace_sys_enter(ksu_reboot_sys_enter, NULL);
+        tracepoint_synchronize_unregister();
+        reboot_trace_registered = false;
+    }
+#endif
     ksu_supercall_cleanup_state();
 }
diff --git a/userspace/ksud/build.rs b/userspace/ksud/build.rs
index 73fb7d28..fe1f1d6b 100644
--- a/userspace/ksud/build.rs
+++ b/userspace/ksud/build.rs
@@ -58,9 +58,11 @@ fn main() {
     let (code, name) = match get_git_version() {
         Ok((code, name)) => (code, name),
         Err(_) => {
-            // show warning if git is not installed
-            println!("cargo:warning=Failed to get git version, using 0.0.0");
-            (0, "0.0.0".to_string())
+            // This target port is built from a source snapshot rather than a
+            // Git worktree. Keep the userspace version aligned with the
+            // v3.2.5 kernel module instead of publishing version zero.
+            println!("cargo:warning=Failed to get git version, using 3.2.5-t870");
+            (32525, "3.2.5-t870".to_string())
         }
     };
     let out_dir = env::var("OUT_DIR").expect("Failed to get $OUT_DIR");
diff --git a/userspace/ksud/src/boot_patch.rs b/userspace/ksud/src/boot_patch.rs
index 00bf575e..2e67a8d1 100644
--- a/userspace/ksud/src/boot_patch.rs
+++ b/userspace/ksud/src/boot_patch.rs
@@ -65,6 +65,13 @@ mod android {
     }
 
     fn parse_kmi(version: &str) -> Result<String> {
+        if version
+            .split_whitespace()
+            .any(|field| field.starts_with("4.19.113-27114284"))
+        {
+            return Ok("4.19.113-27114284".to_string());
+        }
+
         let re = Regex::new(r"(.* )?(\d+\.\d+)(\S+)?(android\d+)(.*)")?;
         let cap = re
             .captures(version)
diff --git a/userspace/ksuinit/src/lib.rs b/userspace/ksuinit/src/lib.rs
index 058db338..1a4a6e0c 100644
--- a/userspace/ksuinit/src/lib.rs
+++ b/userspace/ksuinit/src/lib.rs
@@ -107,8 +107,12 @@ pub fn load_module(data: &[u8], params: &CStr) -> Result<()> {
         unresolved_symbols.insert(name.to_owned(), (sym, offset));
     }
 
+    let mut kernel_text = None;
     if !unresolved_symbols.is_empty() {
         for_each_kernel_symbols(|(symbol, addr)| {
+            if symbol == "_text" {
+                kernel_text = Some(*addr);
+            }
             if let Some((mut sym, offset)) = unresolved_symbols.remove(symbol) {
                 sym.st_shndx = section_header::SHN_ABS as usize;
                 sym.st_value = *addr;
@@ -120,6 +124,27 @@ pub fn load_module(data: &[u8], params: &CStr) -> Result<()> {
         .context("Cannot parse kallsyms")?;
     }
 
+    // Samsung's 4.19 RTIC layout keeps selinux_state in .bss.rtic but omits
+    // its definition from kallsyms.  This fallback is deliberately tied to
+    // the exact SM-T870 T870XXS8DXH1 release and its recovered _text-relative
+    // address; it must never be reused for a different firmware.
+    if unresolved_symbols.contains_key("selinux_state") {
+        let release = fs::read_to_string("/proc/sys/kernel/osrelease")?;
+        if release.trim() == "4.19.113-27114284" {
+            let text = kernel_text.context("Cannot resolve _text for T870 selinux_state")?;
+            let (mut sym, offset) = unresolved_symbols
+                .remove("selinux_state")
+                .context("Missing selinux_state relocation record")?;
+            sym.st_shndx = section_header::SHN_ABS as usize;
+            sym.st_value = text + 0x047cf000;
+            buffer.pwrite_with(sym, offset, ctx)?;
+            log::info!(
+                "Resolved T870XXS8DXH1 selinux_state at {:#x}",
+                sym.st_value
+            );
+        }
+    }
+
     for name in unresolved_symbols.keys() {
         log::warn!("Cannot find symbol: {}", name);
     }
